- release_tracks:
    - GA
  auto_generated: true
  help_text:
    brief: ""
    description: ""
  arguments:
    params:
      - help_text: The project and location for which to retrieve accountConnectors information.
        is_positional: false
        is_primary_resource: true
        resource_spec:
          name: account_connector
          plural_name: accountConnectors
          collection: developerconnect.projects.locations.accountConnectors
          attributes:
            - parameter_name: projectsId
              attribute_name: project
              help: The project id of the {resource} resource.
              property: core/project
            - parameter_name: locationsId
              attribute_name: location
              help: The location id of the {resource} resource.
            - parameter_name: accountConnectorsId
              attribute_name: account_connector
              help: The account_connector id of the {resource} resource.
          disable_auto_completers: false
        required: true
        repeated: false
  request:
    api_version: v1
    collection: developerconnect.projects.locations.accountConnectors.users
  response:
    id_field: name
  output:
    format: |-
      table(
      name,
      displayName,
      createTime,
      lastTokenRequestTime)
