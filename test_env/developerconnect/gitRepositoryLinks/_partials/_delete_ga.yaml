- release_tracks:
    - GA
  auto_generated: true
  help_text:
    brief: Delete a single git repository link
    description: |
      Delete a single git repository link.
    examples: |-
      To delete a git repository link `my-git-repository-link` in a connection `my-connection` in location `us-central1` run:

      $ {command} my-git-repository-link --connection=my-connection --location=us-central1
  arguments:
    params:
      - help_text: The git_repository_link to operate on.
        is_positional: true
        is_primary_resource: true
        resource_spec:
          name: git_repository_link
          plural_name: gitRepositoryLinks
          collection: developerconnect.projects.locations.connections.gitRepositoryLinks
          attributes:
            - parameter_name: projectsId
              attribute_name: project
              help: The project id of the {resource} resource.
              property: core/project
            - parameter_name: locationsId
              attribute_name: location
              help: The location id of the {resource} resource.
            - parameter_name: connectionsId
              attribute_name: connection
              help: The connection id of the {resource} resource.
            - parameter_name: gitRepositoryLinksId
              attribute_name: git_repository_link
              help: The git_repository_link id of the {resource} resource.
          disable_auto_completers: false
        required: true
        repeated: false
      - arg_name: request-id
        api_field: requestId
        help_text: Value for the `request-id` field.
        is_positional: false
        is_primary_resource: false
        required: false
        repeated: false
        type: str
      - arg_name: validate-only
        api_field: validateOnly
        help_text: Value for the `validate-only` field.
        is_positional: false
        is_primary_resource: false
        required: false
        repeated: false
        type: bool
      - arg_name: etag
        api_field: etag
        help_text: Value for the `etag` field.
        is_positional: false
        is_primary_resource: false
        required: false
        repeated: false
        type: str
  request:
    api_version: v1
    collection: developerconnect.projects.locations.connections.gitRepositoryLinks
  async:
    collection: developerconnect.projects.locations.connections.operations
    extract_resource_result: false
