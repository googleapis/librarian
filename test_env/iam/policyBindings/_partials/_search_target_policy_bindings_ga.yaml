- release_tracks:
    - GA
  auto_generated: true
  help_text:
    brief: Search policy bindings by target.
    description: |
      Search policy bindings by target.
    examples: |-
      To search for policy bindings with target, run:

      $ {command} --organization=123 --location=global --target=//cloudresourcemanager.googleapis.com/organizations/123
  arguments:
    params:
      - arg_name: target
        api_field: target
        help_text: Value for the `target` field.
        is_positional: false
        is_primary_resource: false
        required: true
        repeated: false
        type: str
      - arg_name: page-size
        api_field: pageSize
        help_text: Value for the `page-size` field.
        is_positional: false
        is_primary_resource: false
        required: false
        repeated: false
        type: int
      - arg_name: page-token
        api_field: pageToken
        help_text: Value for the `page-token` field.
        is_positional: false
        is_primary_resource: false
        required: false
        repeated: false
        type: str
      - help_text: The parent to operate on.
        is_positional: true
        is_primary_resource: true
        resource_spec:
          name: parent
          plural_name: policyBindings
          collection: iam.organizations.locations.policyBindings
          attributes:
            - parameter_name: organizationsId
              attribute_name: organization
              help: The organization id of the {resource} resource.
            - parameter_name: locationsId
              attribute_name: location
              help: The location id of the {resource} resource.
            - parameter_name: policyBindingsId
              attribute_name: policy_binding
              help: The policy_binding id of the {resource} resource.
          disable_auto_completers: false
        required: true
        repeated: false
  request:
    api_version: v3
    collection:
      - iam.folders.locations.policyBindings
      - iam.organizations.locations.policyBindings
      - iam.projects.locations.policyBindings
    method: searchTargetPolicyBindings
