service_info:
  title: Field Simple Types API
  default_host: fieldsimpletypes.googleapis.com
  package: field_simple_types.v1
  version: v1
  release_tracks: [GA]
proto_info:
  proto_library_path: field_simple_types/v1
  proto_files:
   - field_simple_types.proto
gcloud_info:
  temp_cli_tree_override: field-simple-types
  help_text:
    brief: Manage Field Simple Types API resources.
    description: Manage Field Simple Types API resources.
resource_info:
  resources:
    projects.locations.resources:
      proto_message: Resource
      gcloud_resources:
      - args:
        - arg_name: resource
          is_parent_resource: false
          resource_id_spec: true
        - arg_name: location
          is_parent_resource: true
        - arg_name: project
          is_parent_resource: true
      commands:
        ListResources:
          gcloud_command: resources.list
          gcloud_group_name: resources
        GetResource:
          gcloud_command: resources.describe
          gcloud_group_name: resources
        CreateResource:
          gcloud_command: resources.create
          gcloud_group_name: resources
        UpdateResource:
          gcloud_command: resources.update
          gcloud_group_name: resources
        DeleteResource:
          gcloud_command: resources.delete
          gcloud_group_name: resources
  # Note: from_file flags from gen_sfc gcloud.yaml might need to be added as argument overrides
