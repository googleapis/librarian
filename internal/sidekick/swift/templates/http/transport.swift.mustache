{{!
Copyright 2026 Google LLC

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    https://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
}}
// Code generated by sidekick. DO NOT EDIT.
//
// Copyright {{Codec.Model.CopyrightYear}} Google LLC
{{#Codec.Model.BoilerPlate}}
//{{{.}}}
{{/Codec.Model.BoilerPlate}}

{{#Codec.IsGated}}
#if {{Codec.Name}}
{{/Codec.IsGated}}
import Foundation
{{! On Linux we need this additional import for `URLSession`. }}
#if canImport(FoundationNetworking)
  import FoundationNetworking
#endif
{{#Codec.ServiceImports}}
import {{.}}
{{/Codec.ServiceImports}}
@_spi(GoogleCloudInternal) import GoogleCloudGax

extension Clients {
  final class {{Codec.StubPrefix}}Transport: {{Codec.StubPrefix}}Stub {
    let inner: GoogleCloudGax._HTTPClient

    public init(_ options: GoogleCloudGax.ClientOptions = .init()) throws {
      self.inner = try GoogleCloudGax._HTTPClient(
        from: options,
        withDefaultEndpoint: "https://{{DefaultHost}}",
      )
    }
    {{#Codec.Methods}}

    public func {{> /templates/common/method_signature/request_options}} {
      let (path, query, configure) = try { () throws -> (Swift.String, [URLQueryItem], (inout GoogleCloudGax._HTTPClientRequest) -> Void) in
        {{#Codec.PathBindings}}
        if let candidate = try { () throws -> (Swift.String, [URLQueryItem])? in
          {{#PathVariables}}
          {{#HasMatchingSegments}}
          guard
            let {{Name}} = try GoogleCloudGax._RoutingMatcher.pathValue(
              request{{Expression}},
              matching: {{{MatchingSegmentsExpression}}},
              fieldName: "{{FieldPath}}")
          else {
            return nil
          }
          {{/HasMatchingSegments}}
          {{^HasMatchingSegments}}
          guard let {{Name}} = request{{Expression}}{{#Test}}, {{{.}}}{{/Test}} else {
            return nil
          }
          {{/HasMatchingSegments}}
          {{/PathVariables}}
          let path = "{{PathExpression}}"
          {{#HasQueryParams}}
          var query = [
            URLQueryItem(name: "$alt", value: "{{{ResponseEncoding}}}"),
            {{#APIVersion}}
            URLQueryItem(name: "$apiVersion", value: "{{.}}"),
            {{/APIVersion}}
          ]
          let encoder = GoogleCloudGax._QueryParameterEncoder()
          {{/HasQueryParams}}
          {{^HasQueryParams}}
          let query = [
            URLQueryItem(name: "$alt", value: "{{{ResponseEncoding}}}"),
            {{#APIVersion}}
            URLQueryItem(name: "$apiVersion", value: "{{.}}"),
            {{/APIVersion}}
          ]
          {{/HasQueryParams}}
          {{#QueryParams}}
          {{#IsOneOf}}
          query.append(
            contentsOf: try encoder.encode(
              request.{{Group.Codec.PropertyName}}.flatMap { (oneof) -> {{Codec.BaseFieldType}}? in
                if case let .{{Codec.Name}}(v) = oneof { v } else { nil }
              }, prefix: "{{JSONName}}"))
          {{/IsOneOf}}
          {{^IsOneOf}}
          query.append(contentsOf: try encoder.encode(request.{{Codec.Name}}, prefix: "{{JSONName}}"))
          {{/IsOneOf}}
          {{/QueryParams}}
          return (path, query)
        }() {
          return (candidate.0, candidate.1, { $0.setMethod(.{{HTTPMethod}}) })
        }
        {{/Codec.PathBindings}}
        var paths: [GoogleCloudGax.PathMismatch] = []
        {{#Codec.PathBindings}}
        do {
          {{#HasPathVariables}}
          var builder = GoogleCloudGax._PathMismatchBuilder()
          {{#PathVariables}}
          {{#HasMatchingSegments}}
          builder.maybeAdd(
            request{{Expression}},
            matching: {{{MatchingSegmentsExpression}}},
            fieldName: "{{FieldPath}}",
            expecting: "{{TemplateString}}"
          )
          {{/HasMatchingSegments}}
          {{^HasMatchingSegments}}
          builder.maybeAdd(
            request{{Expression}},
            fieldName: "{{FieldPath}}"
          )
          {{/HasMatchingSegments}}
          {{/PathVariables}}
          paths.append(builder.build())
          {{/HasPathVariables}}
          {{^HasPathVariables}}
          paths.append(GoogleCloudGax.PathMismatch())
          {{/HasPathVariables}}
        }
        {{/Codec.PathBindings}}
        throw GoogleCloudGax.RequestError.binding(GoogleCloudGax.BindingError(paths: paths))
      }()
      var req = try await self.inner.newRequest(percentEncodedPath: path, query: query, options: options)
      configure(&req)
      req.addHeader(name: GoogleCloudGax._HeaderNames.apiClient, value: Clients.clientHeader)
      {{#Codec.HasBody}}
      {{#Codec.IsBodyWildcard}}
      try req.setBody(json: request)
      {{/Codec.IsBodyWildcard}}
      {{^Codec.IsBodyWildcard}}
      if let body = request.{{Codec.BodyField}} {
        try req.setBody(json: body)
      }
      {{/Codec.IsBodyWildcard}}
      {{/Codec.HasBody}}
      {{^ReturnsEmpty}}
      return try await req.rpc(
        {{Codec.ReturnType}}.self, timeout: options.attemptTimeout
      ).get()
      {{/ReturnsEmpty}}
      {{#ReturnsEmpty}}
      _ = try await req.rpc(
        {{Codec.ReturnType}}.self, timeout: options.attemptTimeout
      ).get()
      {{/ReturnsEmpty}}
    }
    {{/Codec.Methods}}
  }
}
{{#Codec.IsGated}}
#endif
{{/Codec.IsGated}}
