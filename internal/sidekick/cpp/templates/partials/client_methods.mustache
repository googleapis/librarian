{{#Codec.Methods}}
{{#Signatures}}

{{{Comments}}}
{{#IsDeprecated}}
  GOOGLE_CLOUD_CPP_DEPRECATED("This RPC is deprecated.")
{{/IsDeprecated}}
{{#IsUnary}}
  {{{CppReturnType}}}
  {{Name}}({{{SigParams}}}Options opts = {});
{{/IsUnary}}
{{#IsPaginated}}
  StreamRange<{{{RangeOutputType}}}>
  {{Name}}({{{SigParams}}}Options opts = {});
{{/IsPaginated}}
{{#IsLongrunning}}
  future<{{{CppReturnType}}}>
  {{Name}}({{{SigParams}}}Options opts = {});

{{{NoAwaitComments}}}
{{#IsDeprecated}}
  GOOGLE_CLOUD_CPP_DEPRECATED("This RPC is deprecated.")
{{/IsDeprecated}}
  StatusOr<{{{LongrunningOperationType}}}>
  {{Name}}(NoAwaitTag, {{{SigParams}}}Options opts = {});
{{/IsLongrunning}}
{{#IsStreamingRead}}
  StreamRange<{{{CppResponseType}}}>
  {{Name}}({{{SigParams}}}Options opts = {});
{{/IsStreamingRead}}
{{#HasIamUpdater}}

  /**
   * Updates the IAM policy for @p resource using an optimistic concurrency
   * control loop.
   *
   * The loop fetches the current policy for @p resource, and passes it to @p
   * updater, which should return the new policy. This new policy should use the
   * current etag so that the read-modify-write cycle can detect races and rerun
   * the update when there is a mismatch. If the new policy does not have an
   * etag, the existing policy will be blindly overwritten. If @p updater does
   * not yield a policy, the control loop is terminated and kCancelled is
   * returned.
   *
   * @param resource  Required. The resource for which the policy is being
   * specified. See the operation documentation for the appropriate value for
   * this field.
   * @param updater  Required. Functor to map the current policy to a new one.
   * @param opts  Optional. Override the class-level options, such as retry and
   *    backoff policies.
   * @return {{{CppResponseType}}}
   */
  StatusOr<{{{CppResponseType}}}>
  {{Name}}(std::string const& resource, IamUpdater const& updater, Options opts = {});
{{/HasIamUpdater}}
{{/Signatures}}
{{#IsUnary}}

{{{RequestComments}}}
{{#IsDeprecated}}
  GOOGLE_CLOUD_CPP_DEPRECATED("This RPC is deprecated.")
{{/IsDeprecated}}
  {{{CppReturnType}}}
  {{Name}}({{{CppRequestType}}} const& request, Options opts = {});
{{/IsUnary}}
{{#IsPaginated}}

{{{RequestComments}}}
{{#IsDeprecated}}
  GOOGLE_CLOUD_CPP_DEPRECATED("This RPC is deprecated.")
{{/IsDeprecated}}
  StreamRange<{{{RangeOutputType}}}>
  {{Name}}({{{CppRequestType}}} request, Options opts = {});
{{/IsPaginated}}
{{#IsLongrunning}}

{{{RequestComments}}}
{{#IsDeprecated}}
  GOOGLE_CLOUD_CPP_DEPRECATED("This RPC is deprecated.")
{{/IsDeprecated}}
  future<{{{CppReturnType}}}>
  {{Name}}({{{CppRequestType}}} const& request, Options opts = {});

{{{NoAwaitComments}}}
{{#IsDeprecated}}
  GOOGLE_CLOUD_CPP_DEPRECATED("This RPC is deprecated.")
{{/IsDeprecated}}
  StatusOr<{{{LongrunningOperationType}}}>
  {{Name}}(NoAwaitTag, {{{CppRequestType}}} const& request, Options opts = {});

{{{OperationComments}}}
{{#IsDeprecated}}
  GOOGLE_CLOUD_CPP_DEPRECATED("This RPC is deprecated.")
{{/IsDeprecated}}
  future<{{{CppReturnType}}}>
  {{Name}}({{{LongrunningOperationType}}} const& operation, Options opts = {});
{{/IsLongrunning}}
{{#IsStreamingRead}}

{{{RequestComments}}}
{{#IsDeprecated}}
  GOOGLE_CLOUD_CPP_DEPRECATED("This RPC is deprecated.")
{{/IsDeprecated}}
  StreamRange<{{{CppResponseType}}}>
  {{Name}}({{{CppRequestType}}} const& request, Options opts = {});
{{/IsStreamingRead}}
{{#IsBidirStreaming}}

{{{Comments}}}
{{#IsDeprecated}}
  GOOGLE_CLOUD_CPP_DEPRECATED("This RPC is deprecated.")
{{/IsDeprecated}}
  std::unique_ptr<::google::cloud::AsyncStreamingReadWriteRpc<
      {{{CppRequestType}}},
      {{{CppResponseType}}}>>
  Async{{Name}}(Options opts = {});
{{/IsBidirStreaming}}
{{/Codec.Methods}}
{{#Codec.AsyncMethods}}
{{#IsUnary}}
{{#Signatures}}

{{{Comments}}}
{{#IsDeprecated}}
  GOOGLE_CLOUD_CPP_DEPRECATED("This RPC is deprecated.")
{{/IsDeprecated}}
  future<{{{CppReturnType}}}>
  Async{{Name}}({{{SigParams}}}Options opts = {});
{{/Signatures}}

{{{RequestComments}}}
{{#IsDeprecated}}
  GOOGLE_CLOUD_CPP_DEPRECATED("This RPC is deprecated.")
{{/IsDeprecated}}
  future<{{{CppReturnType}}}>
  Async{{Name}}({{{CppRequestType}}} const& request, Options opts = {});
{{/IsUnary}}
{{/Codec.AsyncMethods}}
