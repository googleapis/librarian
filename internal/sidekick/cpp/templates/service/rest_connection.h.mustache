{{!
Copyright 2026 Google LLC

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    https://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
}}
{{> /templates/partials/prologue}}
#ifndef {{Codec.RestConnectionHeaderIncludeGuard}}
#define {{Codec.RestConnectionHeaderIncludeGuard}}

#include "{{Codec.ConnectionHeaderPath}}"
#include "google/cloud/options.h"
#include "google/cloud/version.h"
#include <memory>
{{#Codec.IsLocationOptionallyDependent}}
#include <string>
{{/Codec.IsLocationOptionallyDependent}}

namespace google {
namespace cloud {
namespace {{Codec.Namespace}} {
GOOGLE_CLOUD_CPP_INLINE_NAMESPACE_BEGIN

{{#Codec.IsLocationOptionallyDependent}}
/**
 * A factory function to construct an object of type `{{Codec.ConnectionClassName}}`
 * that uses REST over HTTP as transport in lieu of gRPC. REST transport should
 * only be used for services that do not support gRPC or if the existing network
 * configuration precludes using gRPC.
 *
 * The returned connection object should not be used directly; instead it
 * should be passed as an argument to the constructor of {{Codec.ClientClassName}}.
 *
 * The optional @p options argument may be used to configure aspects of the
 * returned `{{Codec.ConnectionClassName}}`. Expected options are any of the types in
 * the following option lists:
 *
 * - `google::cloud::CommonOptionList`
 * - `google::cloud::RestOptionList`
 * - `google::cloud::UnifiedCredentialsOptionList`
 * - `google::cloud::{{Codec.Namespace}}::{{Codec.ServicePolicyOptionListName}}`
 *
 * @note Unexpected options will be ignored. To log unexpected options instead,
 *     set `GOOGLE_CLOUD_CPP_ENABLE_CLOG=yes` in the environment.
 *
 * @param location Sets the prefix for the default `EndpointOption` value.
 * @param options (optional) Configure the `{{Codec.ConnectionClassName}}` created by
 * this function.
 */
std::shared_ptr<{{Codec.ConnectionClassName}}> {{Codec.MakeRestConnectionFunctionName}}(
    std::string const& location, Options options = {});

/**
 * A factory function to construct an object of type `{{Codec.ConnectionClassName}}`.
 *
 * This overload of `{{Codec.MakeRestConnectionFunctionName}}` does not require a location
 * argument, creating a connection to the global service endpoint.
 */
std::shared_ptr<{{Codec.ConnectionClassName}}> {{Codec.MakeRestConnectionFunctionName}}(
    Options options = {});
{{/Codec.IsLocationOptionallyDependent}}
{{^Codec.IsLocationOptionallyDependent}}
/**
 * A factory function to construct an object of type `{{Codec.ConnectionClassName}}`
 * that uses REST over HTTP as transport in lieu of gRPC. REST transport should
 * only be used for services that do not support gRPC or if the existing network
 * configuration precludes using gRPC.
 *
 * The returned connection object should not be used directly; instead it
 * should be passed as an argument to the constructor of {{Codec.ClientClassName}}.
 *
 * The optional @p options argument may be used to configure aspects of the
 * returned `{{Codec.ConnectionClassName}}`. Expected options are any of the types in
 * the following option lists:
 *
 * - `google::cloud::CommonOptionList`
 * - `google::cloud::RestOptionList`
 * - `google::cloud::UnifiedCredentialsOptionList`
 * - `google::cloud::{{Codec.Namespace}}::{{Codec.ServicePolicyOptionListName}}`
 *
 * @note Unexpected options will be ignored. To log unexpected options instead,
 *     set `GOOGLE_CLOUD_CPP_ENABLE_CLOG=yes` in the environment.
 *
 * @param options (optional) Configure the `{{Codec.ConnectionClassName}}` created by
 * this function.
 */
std::shared_ptr<{{Codec.ConnectionClassName}}> {{Codec.MakeRestConnectionFunctionName}}(
    Options options = {});
{{/Codec.IsLocationOptionallyDependent}}

GOOGLE_CLOUD_CPP_INLINE_NAMESPACE_END
}  // namespace {{Codec.Namespace}}
}  // namespace cloud
}  // namespace google

#endif  // {{Codec.RestConnectionHeaderIncludeGuard}}
