title: Delete a resource with help text
release_tracks: [GA]
summary:
# This summary is generated by http://go/gcloud-scenario-tests on update and should not be edited.
- set_property: core/project my-project
- execute:
  - label: test can run delete command with all flags
  - command: auto help-text resources delete $$resource$$ --location=$$location$$
      --etag=my-etag --validate-only --request-id=my-id
  - prompt:
    - message: You are about to delete resource [$$resource$$]
    - input: y
  - stderr: |
      Delete request issued for: [$$resource$$]
      Deleted resource [$$resource$$].
- execute:
  - label: test delete command help text
  - command: auto help-text resources delete --help
  - stdout: |
      NAME
          gcloud auto help-text resources delete - delete resources

      SYNOPSIS
          gcloud auto help-text resources delete (RESOURCE : --location=LOCATION)
              [--async] [--etag=ETAG] [--request-id=REQUEST_ID] [--validate-only]
              [GCLOUD_WIDE_FLAG ...]

      DESCRIPTION
          Delete a resource

      EXAMPLES
          To delete the resource, run:

              $ gcloud auto help-text resources delete

      POSITIONAL ARGUMENTS
           Resource resource - The resource name of the resource within a service.
           The arguments in this group can be used to specify the attributes of this
           resource. (NOTE) Some attributes are not given arguments in this group but
           can be set in other ways.

           To set the project attribute:
            ◆ provide the argument resource on the command line with a fully
              specified name;
            ◆ provide the argument --project on the command line;
            ◆ set the property core/project.

           This must be specified.

             RESOURCE
                ID of the resource or fully qualified identifier for the resource.

                To set the resource attribute:
                ▸ provide the argument resource on the command line.

                This positional argument must be specified if any of the other
                arguments in this group are specified.

             --location=LOCATION
                The location id of the resource resource.

                To set the location attribute:
                ▸ provide the argument resource on the command line with a fully
                  specified name;
                ▸ provide the argument --location on the command line.

      FLAGS
           --async
              Return immediately, without waiting for the operation in progress to
              complete.

           --etag=ETAG
              The etag known to the client for the expected state of the resource.
              This is used with state-changing methods to prevent accidental
              overwrites when multiple user agents might be acting in parallel on the
              same resource.

              An etag wildcard provide optimistic concurrency based on the expected
              existence of the resource. The Any wildcard (*) requires that the
              resource must already exists, and the Not Any wildcard (!*) requires
              that it must not.

           --request-id=REQUEST_ID
              An optional request ID to identify requests. Specify a unique request
              ID so that if you must retry your request, the server will know to
              ignore the request if it has already been completed. The server will
              guarantee that for at least 60 minutes since the first request.

              For example, consider a situation where you make an initial request and
              the request times out. If you make the request again with the same
              request ID, the server can check if original operation with the same
              request ID was received, and if so, will ignore the second request.
              This prevents clients from accidentally creating duplicate commitments.

              The request ID must be a valid UUID with the exception that zero UUID
              is not supported (00000000-0000-0000-0000-000000000000).

           --validate-only
              If "validate_only" is set to true, the service will try to validate
              that this request would succeed, but will not actually make changes.

      GCLOUD WIDE FLAGS
          These flags are available to all commands: --access-token-file, --account,
          --billing-project, --configuration, --flags-file, --flatten, --format,
          --help, --impersonate-service-account, --log-http, --project, --quiet,
          --trace-token, --user-output-enabled, --verbosity.

          Run $ gcloud help for details.

      API REFERENCE
          This command uses the helptext/v1 API. The full documentation for this API
          can be found at:
  - error: '1: 0'
actions:
- set_property:
    core/project: my-project
- define_reference:
    reference: location
    value: us-east1
- define_reference:
    reference: resource
    value: my-resource

- execute_command:
    label: test can run delete command with all flags
    command: auto help-text resources delete $$resource$$ --location=$$location$$
      --etag=my-etag --validate-only --request-id=my-id
    events:
    - expect_prompt_continue:
        message: You are about to delete resource [$$resource$$]
        user_input: y
    - api_call:
        expect_request:
          uri: https://helptext.$$UNIVERSE_DOMAIN$$/v1/projects/my-project/locations/$$location$$/resources/$$resource$$?alt=json&etag=my-etag&requestId=my-id&validateOnly=True
          method: DELETE
          headers: {}
          body: null
        return_response:
          status: 200
          headers: {}
          body:
            done: true
            name: projects/fake-project/locations/us-central1/operations/mock_operation
        poll_operation: true
    - expect_stderr: |
        Delete request issued for: [$$resource$$]
        Deleted resource [$$resource$$].
    - expect_exit:
        code: 0
- execute_command:
    label: test delete command help text
    command: auto help-text resources delete --help
    events:
    - expect_stdout: |
        NAME
            gcloud auto help-text resources delete - delete resources

        SYNOPSIS
            gcloud auto help-text resources delete (RESOURCE : --location=LOCATION)
                [--async] [--etag=ETAG] [--request-id=REQUEST_ID] [--validate-only]
                [GCLOUD_WIDE_FLAG ...]

        DESCRIPTION
            Delete a resource

        EXAMPLES
            To delete the resource, run:

                $ gcloud auto help-text resources delete

        POSITIONAL ARGUMENTS
             Resource resource - The resource name of the resource within a service.
             The arguments in this group can be used to specify the attributes of this
             resource. (NOTE) Some attributes are not given arguments in this group but
             can be set in other ways.

             To set the project attribute:
              ◆ provide the argument resource on the command line with a fully
                specified name;
              ◆ provide the argument --project on the command line;
              ◆ set the property core/project.

             This must be specified.

               RESOURCE
                  ID of the resource or fully qualified identifier for the resource.

                  To set the resource attribute:
                  ▸ provide the argument resource on the command line.

                  This positional argument must be specified if any of the other
                  arguments in this group are specified.

               --location=LOCATION
                  The location id of the resource resource.

                  To set the location attribute:
                  ▸ provide the argument resource on the command line with a fully
                    specified name;
                  ▸ provide the argument --location on the command line.

        FLAGS
             --async
                Return immediately, without waiting for the operation in progress to
                complete.

             --etag=ETAG
                The etag known to the client for the expected state of the resource.
                This is used with state-changing methods to prevent accidental
                overwrites when multiple user agents might be acting in parallel on the
                same resource.

                An etag wildcard provide optimistic concurrency based on the expected
                existence of the resource. The Any wildcard (*) requires that the
                resource must already exists, and the Not Any wildcard (!*) requires
                that it must not.

             --request-id=REQUEST_ID
                An optional request ID to identify requests. Specify a unique request
                ID so that if you must retry your request, the server will know to
                ignore the request if it has already been completed. The server will
                guarantee that for at least 60 minutes since the first request.

                For example, consider a situation where you make an initial request and
                the request times out. If you make the request again with the same
                request ID, the server can check if original operation with the same
                request ID was received, and if so, will ignore the second request.
                This prevents clients from accidentally creating duplicate commitments.

                The request ID must be a valid UUID with the exception that zero UUID
                is not supported (00000000-0000-0000-0000-000000000000).

             --validate-only
                If "validate_only" is set to true, the service will try to validate
                that this request would succeed, but will not actually make changes.

        GCLOUD WIDE FLAGS
            These flags are available to all commands: --access-token-file, --account,
            --billing-project, --configuration, --flags-file, --flatten, --format,
            --help, --impersonate-service-account, --log-http, --project, --quiet,
            --trace-token, --user-output-enabled, --verbosity.

            Run $ gcloud help for details.

        API REFERENCE
            This command uses the helptext/v1 API. The full documentation for this API
            can be found at:
    - expect_exit:
        code: 1
        message: '0'
