title: List a resource in multiservice
release_tracks: [GA]
summary:
# This summary is generated by http://go/gcloud-scenario-tests on update and should not be edited.
- set_property: core/project my-project
- execute:
  - label: test list first resources
  - command: auto multi-service firsts resources list --location=$$location$$ --first=$$first$$
      --limit=1
  - stdout: |
      ---
      name: mock_name
      resourceOneField: mock_resource_one_field
actions:
- set_property:
    core/project: my-project
- define_reference:
    reference: location
    value: us-east1
- define_reference:
    reference: first
    value: my-first

- execute_command:
    label: test list first resources
    command: auto multi-service firsts resources list --location=$$location$$ --first=$$first$$
      --limit=1
    events:
    - api_call:
        expect_request:
          uri: https://multiservice.$$UNIVERSE_DOMAIN$$/v1/projects/my-project/locations/$$location$$/firsts/$$first$$/resources?alt=json
          method: GET
          headers: {}
          body: null
        return_response:
          status: 200
          headers: {}
          body:
            firstResources:
            - name: mock_name
              resourceOneField: mock_resource_one_field
            nextPageToken: mock_next_page_token
            unreachable:
            - mock_unreachable
    - expect_stdout: |
        ---
        name: mock_name
        resourceOneField: mock_resource_one_field
    - expect_exit:
        code: 0
