title: Describe a hidden command
release_tracks: [GA]
summary:
# This summary is generated by http://go/gcloud-scenario-tests on update and should not be edited.
- execute:
  - label: test hidden flag not in help text
  - command: auto hidden-feature visible-resources create --help
  - stdout: |
      NAME
          gcloud auto hidden-feature visible-resources create - create
              visibleResources

      SYNOPSIS
          gcloud auto hidden-feature visible-resources create
              (VISIBLE_RESOURCE : --location=LOCATION) [--labels=[LABELS,...]]
              [--request-id=REQUEST_ID] [--validate-only] [GCLOUD_WIDE_FLAG ...]

      DESCRIPTION
          Create a visibleResource

      EXAMPLES
          To create the visibleResource, run:

              $ gcloud auto hidden-feature visible-resources create

      POSITIONAL ARGUMENTS
           VisibleResource resource - Identifier. Resource name. The arguments in
           this group can be used to specify the attributes of this resource. (NOTE)
           Some attributes are not given arguments in this group but can be set in
           other ways.

           To set the project attribute:
            ◆ provide the argument visible_resource on the command line with a
              fully specified name;
            ◆ provide the argument --project on the command line;
            ◆ set the property core/project.

           This must be specified.

             VISIBLE_RESOURCE
                ID of the visibleResource or fully qualified identifier for the
                visibleResource.

                To set the visible_resource attribute:
                ▸ provide the argument visible_resource on the command line.

                This positional argument must be specified if any of the other
                arguments in this group are specified.

             --location=LOCATION
                The location id of the visibleResource resource.

                To set the location attribute:
                ▸ provide the argument visible_resource on the command line with a
                  fully specified name;
                ▸ provide the argument --location on the command line.

      FLAGS
           --labels=[LABELS,...]
              The labels on the resource, which can be used for categorization.
              similar to Kubernetes resource labels.

               KEY
                  Keys must start with a lowercase character and contain only hyphens
                  (-), underscores (_), lowercase characters, and numbers.

               VALUE
                  Values must contain only hyphens (-), underscores (_), lowercase
                  characters, and numbers.

              Shorthand Example:

                  --labels=string=string

              JSON Example:

                  --labels='{"string": "string"}'

              File Example:

                  --labels=path_to_file.(yaml|json)

           --request-id=REQUEST_ID
              An optional request ID to identify requests. Specify a unique request
              ID so that if you must retry your request, the server will know to
              ignore the request if it has already

           --validate-only
              If "validate_only" is set to true, the service will try to validate
              that this request would succeed, but will not actually make changes.

      GCLOUD WIDE FLAGS
          These flags are available to all commands: --access-token-file, --account,
          --billing-project, --configuration, --flags-file, --flatten, --format,
          --help, --impersonate-service-account, --log-http, --project, --quiet,
          --trace-token, --user-output-enabled, --verbosity.

          Run $ gcloud help for details.

      API REFERENCE
          This command uses the hiddenfeature/v1 API. The full documentation for this
          API can be found at:
  - error: '1: 0'
actions:

- execute_command:
    label: test hidden flag not in help text
    command: auto hidden-feature visible-resources create --help
    events:
    - expect_stdout: |
        NAME
            gcloud auto hidden-feature visible-resources create - create
                visibleResources

        SYNOPSIS
            gcloud auto hidden-feature visible-resources create
                (VISIBLE_RESOURCE : --location=LOCATION) [--labels=[LABELS,...]]
                [--request-id=REQUEST_ID] [--validate-only] [GCLOUD_WIDE_FLAG ...]

        DESCRIPTION
            Create a visibleResource

        EXAMPLES
            To create the visibleResource, run:

                $ gcloud auto hidden-feature visible-resources create

        POSITIONAL ARGUMENTS
             VisibleResource resource - Identifier. Resource name. The arguments in
             this group can be used to specify the attributes of this resource. (NOTE)
             Some attributes are not given arguments in this group but can be set in
             other ways.

             To set the project attribute:
              ◆ provide the argument visible_resource on the command line with a
                fully specified name;
              ◆ provide the argument --project on the command line;
              ◆ set the property core/project.

             This must be specified.

               VISIBLE_RESOURCE
                  ID of the visibleResource or fully qualified identifier for the
                  visibleResource.

                  To set the visible_resource attribute:
                  ▸ provide the argument visible_resource on the command line.

                  This positional argument must be specified if any of the other
                  arguments in this group are specified.

               --location=LOCATION
                  The location id of the visibleResource resource.

                  To set the location attribute:
                  ▸ provide the argument visible_resource on the command line with a
                    fully specified name;
                  ▸ provide the argument --location on the command line.

        FLAGS
             --labels=[LABELS,...]
                The labels on the resource, which can be used for categorization.
                similar to Kubernetes resource labels.

                 KEY
                    Keys must start with a lowercase character and contain only hyphens
                    (-), underscores (_), lowercase characters, and numbers.

                 VALUE
                    Values must contain only hyphens (-), underscores (_), lowercase
                    characters, and numbers.

                Shorthand Example:

                    --labels=string=string

                JSON Example:

                    --labels='{"string": "string"}'

                File Example:

                    --labels=path_to_file.(yaml|json)

             --request-id=REQUEST_ID
                An optional request ID to identify requests. Specify a unique request
                ID so that if you must retry your request, the server will know to
                ignore the request if it has already

             --validate-only
                If "validate_only" is set to true, the service will try to validate
                that this request would succeed, but will not actually make changes.

        GCLOUD WIDE FLAGS
            These flags are available to all commands: --access-token-file, --account,
            --billing-project, --configuration, --flags-file, --flatten, --format,
            --help, --impersonate-service-account, --log-http, --project, --quiet,
            --trace-token, --user-output-enabled, --verbosity.

            Run $ gcloud help for details.

        API REFERENCE
            This command uses the hiddenfeature/v1 API. The full documentation for this
            API can be found at:
    - expect_exit:
        code: 1
        message: '0'
