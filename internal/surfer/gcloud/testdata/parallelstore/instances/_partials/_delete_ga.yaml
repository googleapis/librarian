- release_tracks:
    - GA
  auto_generated: true
  hidden: true
  help_text:
    brief: Deletes a single Parallelstore instance
    description: |
      Deletes a single Parallelstore instance.
    examples: |-
      To delete an instance `my-instance` run:

      $ {command} my-instance
  arguments:
    params:
      - arg_name: request-id
        api_field: requestId
        help_text: Value for the `request_id` field.
        is_positional: false
        is_primary_resource: false
        required: false
        repeated: false
        type: str
  request:
    api_version: v1alpha
    collection:
      - parallelstore.projects.locations.instances
  async:
    collection:
      - parallelstore.projects.locations.operations
    extract_resource_result: false
